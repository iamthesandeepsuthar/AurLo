import { UserChangePassword } from './user-change-password.model';

describe('UserChangePassword', () => {
  it('should create an instance', () => {
    expect(new UserChangePassword()).toBeTruthy();
  });
});
